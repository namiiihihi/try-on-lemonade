# /today — Daily standup summary

Show me what's happening today on the Try On Lemonteen project:

1. Check `git log --oneline -10` for recent commits
2. Check `git status` for uncommitted changes
3. List any failing tests with `npm run test -- --passWithNoTests 2>&1 | tail -20`
4. Check Supabase migration status
5. Summarize in 3 bullets: ✅ Done yesterday / 🔨 In progress / 🎯 Today's focus

Keep the summary under 150 words. Use Vietnamese.
